---

import BaseLayout from "../layouts/BaseLayout.astro";
import Header from '../components/Header.astro';
const { frontmatter } = Astro.props;
---

<BaseLayout pageTitle={frontmatter.title} frontmatter={frontmatter}>
	<a href="/blog" class="text-xs text-neutral-300">back</a>
	<h1 class="text-2xl lg:text-3xl my-16">{frontmatter.title}</h1>
	<article class="prose-sm prose-invert prose-neutral max-w-[80ch] leading-8 prose-hr:border-neutral-800">
		<slot />
	</article>
</BaseLayout>